#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <string>
#include <sstream>
using namespace std;


int main() {
	/* Enter your code here. Read input from STDIN. Print output to STDOUT */
	int line_num;
	int query_num;
	cin >> line_num >> query_num;

	string str;
	char ch1, ch2;
	int a, b;
	string s1,s2,s3;
	while (line_num != 0)
	{
		cin >> str;
		stringstream ss(str);
		ss >> ch1 >> s1 >> ch2;
		if (ch1 == '<')
		{

		}
		line_num--;
	}
	return 0;
}
